pull_request_rules:
    - name: Merge all pull requests
      conditions:
        - or:
            - files=package.csproj
      actions:
         merge:
           method: merge
    - name: Delete Branch After Merge
      conditions:
        - merged
      actions:
         delete_head_branch: {}